apiVersion: apps/v1
kind: Deployment
metadata:
  name: llamastack
  namespace: llamastack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: llamastack
  template:
    metadata:
      labels:
        app: llamastack
    spec:
      containers:
        - name: llamastack
          image: docker.io/llamastack/distribution-starter:0.2.24
          command: ["python", "-m", "llama_stack.core.server.server"]
          args: ["/config/run.yaml"]
          ports:
            - containerPort: 8321
          env:
            - name: AZURE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: azure-openai-secret
                  key: AZURE_API_KEY
            - name: AZURE_API_BASE
              valueFrom:
                secretKeyRef:
                  name: azure-openai-secret
                  key: AZURE_API_BASE
            - name: AZURE_API_VERSION
              valueFrom:
                secretKeyRef:
                  name: azure-openai-secret
                  key: AZURE_API_VERSION
            - name: AZURE_API_TYPE
              value: "azure"
          volumeMounts:
            - name: run-config
              mountPath: /config
      volumes:
        - name: run-config
          configMap:
            name: llamastack-run-config