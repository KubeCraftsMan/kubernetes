apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-stack-server
  namespace: llamastack
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: llama-stack
  template:
    metadata:
      labels:
        app.kubernetes.io/name: llama-stack
    spec:
      containers:
        - name: llama-stack
          image: brcaetano/vllm-cpu-env:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: HF_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hf-token
                  key: HF_TOKEN   # corrigido para a chave correta do secret
            - name: VLLM_CPU_KVCACHE_SPACE
              value: "40"
            - name: VLLM_CPU_OMP_THREADS_BIND
              value: "0-3"   # ajuste de acordo com os núcleos disponíveis no pod
            - name: VLLM_CPU_NUM_OF_RESERVED_CPU
              value: "1"
          args:
            - "--model"
            - "meta-llama/Llama-3.2-1B-Instruct"
            - "--dtype"
            - "bfloat16"
            - "--max-num-batched-tokens"
            - "131072"
            - "--disable-gpu"
          securityContext:
            capabilities:
              add:
                - SYS_NICE
            seccompProfile:
              type: Unconfined
