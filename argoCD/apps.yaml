apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: apps-from-repo
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/KubeCraftsMan/kubernetes.git
        revision: HEAD
        directories:
          - path: '*'
  template:
    metadata:
      name: '{{path.basename | toLower}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/KubeCraftsMan/kubernetes.git
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{path.basename | toLower}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
